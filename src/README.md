# Scientific analysis source

- `chemistry_ready_mof_analysis_pipeline_v2_2_claim_ready_sciml_visual_final.py` — resume-aware historical analysis pipeline.
- `make_publication_assets_v2_3.py` — historical publication-asset generator retained for provenance.
- `requirements_historical_unpinned.txt` — dependency names supplied with the original code package.

For the reconstructed historical environment and exact recorded run configuration, see `environment/historical_analysis.yml` and `docs/HISTORICAL_RUN.md`.

The final manuscript figure renderer is separate and lives under `reproduce/figures/`.
